我们基于 MSIT 框架为盘古（Pangu）模型开发了量化适配代码，该代码支持多比特量化模式（A8W8、A4W4）及多种量化算法，可根据硬件算力、内存容量、模型规模等实际场景需求灵活选型。同时，我们已完成量化推理功能的适配支持，具体实现细节可参考本模型部署部分。

具体使用方式如下：在`msit/msmodelslim/example`目录下新建`Pangu`子目录，将`quant_pangu.py`脚本与`quant.sh`执行脚本放入该目录后，运行`quant.sh`即可完成量化操作；其中脚本内的模型路径可根据实际部署的模型存储位置灵活调整。
